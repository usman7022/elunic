import { Route } from '@angular/router';

export const appRoutes: Route[] = [
  // Empty routes array - we'll let the AppComponent handle all content
  // This prevents the duplicate component issue
];
